repos:
  - repo: local
    hooks:
      - id: ruff-format
        name: Ruff format
        entry: uv run --group dev ruff format
        language: system
        types_or: [python, pyi]
      - id: ruff-check
        name: Ruff check (fix)
        entry: uv run --group dev ruff check --fix
        language: system
        types_or: [python, pyi]
