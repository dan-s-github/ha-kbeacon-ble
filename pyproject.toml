[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ha-kbeacon-ble"
version = "1.0.0rc1"
description = "Home Assistant integration for KBeacon BLE devices"
readme = "README.md"
license = { text = "MIT" }
authors = [
    { name = "Daniel M", email = "daniel@mohni.info" },
]
requires-python = ">=3.14.2"
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Plugins",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Natural Language :: English",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.14",
]
dependencies = [
    "kbeacon-ble>=1.0.0rc5",
]

[dependency-groups]
dev = [
    "aiousbwatcher",
    "bluetooth-adapters>=2.1.0",
    "commitizen>=3.29.1",
    "colorlog>=6.10.1",
    # habluetooth, serialx, and aioesphomeapi are pinned as an exception to the
    # "rely on Home Assistant's dependency set" policy below: Home Assistant
    # 2026.7.0's bluetooth component hard-requires habluetooth==6.25.1, and
    # loading it transitively imports the usb component (serialx==1.8.2),
    # which in turn imports its ESPHome serial platform (aioesphomeapi==45.3.1)
    # at module scope. None of these are declared as install-time package
    # dependencies of the homeassistant wheel, so uv can't resolve them
    # without an explicit pin.
    "habluetooth==6.25.1",
    "homeassistant==2026.7.0",
    "pycares>=5.0.0,<6",
    "pytest>=8.2.0",
    "pytest-asyncio>=1.3.0",
    "pytest-homeassistant-custom-component==0.13.344",
    "pyserial",
    "serialx==1.8.2",
    "aioesphomeapi==45.3.1",
    "ruff>=0.15.0",
    "mypy>=1.8.0",
    "pre-commit>=4.5.1",
]

[tool.hatch.build.targets.wheel]
packages = ["custom_components/kbeacon"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
minversion = "8.2"

[tool.mypy]
python_version = "3.14"
strict = false
warn_unused_ignores = true
warn_redundant_casts = true
warn_unused_configs = true
ignore_missing_imports = true

[tool.commitizen]
name = "cz_conventional_commits"
version_provider = "pep621"
version_files = ["custom_components/kbeacon/manifest.json:version"]
tag_format = "v$version"
update_changelog_on_bump = true
